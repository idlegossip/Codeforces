#include<bits/stdc++.h>
using namespace std;
#define ll long long
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    ll t;
    cin>>t;
    while(t--)
    {
        ll n;
        cin>>n;
        ll a;
        ll tar = (n * n)  - 1;
        ll ar[n+1][n+1];
        ar[1][1] = 1;
        for(int i = 1; i <= n; i++)
        {
            for(int j = 1; j <= n; j++)
            {
                if(i== j and i == 1)
                    continue;
                if(i==1){
                    ar[]
                }

            }
        }
    }
}
 
